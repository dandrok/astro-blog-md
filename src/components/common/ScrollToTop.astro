---

---

<button
  id="scroll-to-top"
  aria-label="Scroll to top"
  class="fixed bottom-6 z-40 hidden border border-green-400/30 bg-black/90 px-4 py-2 text-sm text-green-400 transition-colors duration-200 hover:bg-black hover:text-green-200 focus:ring-2 focus:ring-green-500/50 focus:outline-none"
>
  <span class="sr-only">Scroll to top</span>
  <span aria-hidden="true" class="tracking-[0.25em] uppercase">$ top</span>
</button>

<script>
  const scrollBtn = document.getElementById('scroll-to-top');
  const mainContent = document.getElementById('main-content'); // Assuming main-content ID is set in layout

  function setButtonPosition() {
    if (scrollBtn && mainContent) {
      if (window.innerWidth < 1024) {
        // Mobile/Tablet: keep it in the lower-right corner.
        scrollBtn.style.right = '1rem';
        scrollBtn.style.left = 'auto';
      } else {
        // Desktop: place it in the right gutter beside the main content.
        const mainRect = mainContent.getBoundingClientRect();
        const rightOffset = Math.max(24, window.innerWidth - mainRect.right - 24);

        scrollBtn.style.right = `${rightOffset}px`;
        scrollBtn.style.left = 'auto';
      }
    }
  }

  if (scrollBtn && mainContent) {
    // Initial position setup
    setButtonPosition();

    // Recalculate position on window resize
    window.addEventListener('resize', setButtonPosition);

    // Toggle visibility based on scroll position
    window.addEventListener('scroll', () => {
      if (window.scrollY > 300) {
        scrollBtn.classList.remove('hidden');
        scrollBtn.classList.add('block');
      } else {
        scrollBtn.classList.add('hidden');
        scrollBtn.classList.remove('block');
      }
    });

    // Smooth scroll to top
    scrollBtn.addEventListener('click', () => {
      window.scrollTo({
        top: 0,
        behavior: 'smooth',
      });
    });
  }
</script>
